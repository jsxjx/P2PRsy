from django.conf.urls import url
from django.contrib import admin

from p2p_website import views

urlpatterns = [
    url(r'^$', views.index),
    url(r'^index/$', views.index, name="index"),
    url(r'^borrower/$', views.borrower, name="borrower"),
]
